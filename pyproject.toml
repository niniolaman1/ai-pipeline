[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "citypulse"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "gtfs-realtime-bindings",
    "python-dotenv",
    "psycopg2-binary"
]
