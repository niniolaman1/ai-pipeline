services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: devpassword
    volumes:
      - citypulse-db-data:/var/lib/postgresql/data

  bus-collector:
    build: .
    depends_on:
      - postgres

volumes:
  citypulse-db-data:
